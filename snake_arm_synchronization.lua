--this file contains functions synchronizing (1) data and GUI; (2) vrep data and real robot


function jointIdToSliderId(jointId)
	return jointId + 20
end

function synchronizeOneAngleSlider(jointId, newValue)
	
end